import { Box, Typography } from "@material-ui/core";
import React from "react";

const WeatherCard = () => {
  return (
    <Box>
      <Typography>Weather block</Typography>
    </Box>
  );
};

export default WeatherCard;
